// import { useEffect } from "react";

// function useThrottle(cb: ({...args}) => void, interval = 500) {
//   let isNewData=false;
//   useEffect(() => {
//     if (isNewData) {
//
//     } else {
//       const timoutId = setTimeout(() => {}, interval);
//       return () => clearTimeout(timeoutId);
//     }
//   }, [callback, interval]);
// }
